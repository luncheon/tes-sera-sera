{
  "name": "tes-sera-sera",
  "version": "0.0.0",
  "description": "A tesseract.js camera example.",
  "private": true,
  "scripts": {
    "start": "esbuild --bundle --outfile=app.js app.ts --serve=0.0.0.0:8000 --servedir=.",
    "build": "esbuild --bundle --outfile=app.js app.ts"
  },
  "devDependencies": {
    "esbuild": "^0.17.14"
  },
  "dependencies": {
    "tesseract.js": "^4.0.2"
  }
}
